// Espera a que el documento esté completamente cargado
document.addEventListener('DOMContentLoaded', () => {

  // El código anterior fue eliminado porque:
  // 1. La función alert() puede ser intrusiva para la experiencia del usuario.
  // 2. Se buscaban IDs ('tu-id-de-boton', 'btn-volver') que no existen en el HTML,
  //    por lo que ese código no tenía efecto.
  // Los botones de contacto en el HTML ya funcionan correctamente con etiquetas <a>
  // y no necesitan JavaScript adicional.

  console.log('✅ Script cargado correctamente.');
});
